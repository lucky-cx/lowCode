// 物料到画布，组件对应渲染
export enum MATERIAL_TO_COMPENT {
  TEXT = "t-input",
  SELECT = "t-select",
  NUMBER = "t-input-number",
}
// 属性面板，属性到组件动态生成，组件对应
export enum M_ATTRS_TO_COMPENT {
  TEXT = "t-input",
  SELECT = "t-select",
  NUMBER = "t-input-number",
  OBJECT = "object",
}
